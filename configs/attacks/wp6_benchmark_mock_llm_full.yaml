experiment:
  name: wp6_mock_llm_overhead_full
  seed: 42
  domain: front_office
  workflow: front_office_transaction_execution
  scenario: front_office_transaction_execution

observability:
  mode: full
  export: [jsonl]

benchmark:
  # concurrency: 1 -- see the comment in wp6_benchmark_mock_llm_off.yaml.
  concurrency: 1
  repetitions: 20
  mock_llm: true
