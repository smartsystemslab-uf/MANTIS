experiment:
  name: wp6_mock_llm_overhead_back_office
  seed: 42
  domain: back_office
  workflow: back_office_clean
  scenario: back_office_clean

observability:
  mode: full
  export: [jsonl]

benchmark:
  # Mirrors wp6_benchmark_mock_llm_full.yaml (front_office) for a
  # back_office workflow, closing the spec §WP6 requirement to benchmark
  # "representative front-, mid-, and back-office workflows" rather than
  # front_office alone.
  concurrency: 1
  repetitions: 20
  mock_llm: true
